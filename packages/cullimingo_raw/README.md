# cullimingo_raw (placeholder)

Future home of the native FFI layer (LibRaw for RAW decode/preview, libvips for
fast resize/encode). Per `BUILD_PLAN.md` §3/§6.1 we start in-app and extract the
native code here once it stabilises, to keep build pain out of the app package.
Not yet a published/path package.
